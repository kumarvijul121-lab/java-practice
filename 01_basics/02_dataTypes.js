"use strict"; // Treats all JS code as newer version and throws error for unsafe actions

alert("Hello World") // we are using nodejs so alert will not work here , only works in browser

.console.log(3 
      +
       3) //code readability should be high

    console.log (" Viul")

    let name = "Vijul"
    let = age = 19
   let isloggedIn = true 


   // number => 1, 3.14, -5, etc
   // bigint => 9007199254740991n (for very large integers)
   // boolean => true/false
   // null => standalone value that represents "nothing"
   // undefined => variable that is not assigned a value yet
   // symbol => unique identifier for obects (use Symbol() to create)




   //Object => collection of key value pairs

   console.log(typeof "Vijul")

